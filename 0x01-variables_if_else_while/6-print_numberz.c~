#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */

int main(void)
{
	int i;
	/*commet*/
	for (i = 0; i < 9; i++)
		/*commey*/
		putchar("%d", i);
	putchar("\n");
	return (0);
}
